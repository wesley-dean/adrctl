# Reference Documentation

This directory contains source-reference documentation generated from the
maintained Bash implementation.

Do not edit generated reference files manually.

Regenerate the reference documentation with:

```bash
make docs
```

Use `make docs-stage` when the generated documentation should be staged together
with the source change that produced it.
